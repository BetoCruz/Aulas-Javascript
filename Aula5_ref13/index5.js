const nome = prompt("Digite seu nome coompleto:");
        document.body.innerHTML += ` O  seu nome é : ${nome}<br/>`;
        document.body.innerHTML += `Seu nome tem qtas letras: ${nome.length}<br/>`;
        document.body.innerHTML += `A segunda letra do seu nome é: ${nome[1]}<br/>`;
        document.body.innerHTML +=  `Qual o primeiro indice da letra "e" do seu nome:${nome.indexOf("e")}<br/>`;  
        document.body.innerHTML += `Qual o último indice da letra "o" do seu nome :${nome.lastIndexOf("o")}<br/>`;       
        document.body.innerHTML += `As últimas tres letras do seu nome são:${nome.slice(-3,nome.length)}<br/>`;
        document.body.innerHTML += `As paçavras do seu nome são :${nome.split(" ")} <br/>`;
        document.body.innerHTML += `Seu nome com letras maiúsculas${nome.toUpperCase()} <br/>`;        
        document.body.innerHTML += `Seu nome com letras minúsculas:${nome.toLowerCase()} <br/>`;